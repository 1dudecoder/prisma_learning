import {createUser, getUserById, getAllUser, deleteUser, updateUser} from "../controller/userController";

export const usercontroller = {
    createUser,
    getUserById,
    getAllUser,
    deleteUser,
    updateUser
}